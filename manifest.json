{
	"name": "Dreamwidth Filter",
	"version": "0.6",
	"description": "Filters Dreamwidth entry pages such that the top-level comments shown are the ones the user is actively searching for in the subject headers.",
	"manifest_version": 2,
	"browser_action": {
		"default_popup": "popup.html"
	},
	"options_page": "options.html",
	"permissions": [
		"storage",
		"tabs"
	],
	"content_scripts": [{
		"matches": [
			//"*://*.dreamwidth.org/*"
			"<all_urls>"
		],
		"css": ["content.css"],
		"js": ["content.js"]
	}]
}
